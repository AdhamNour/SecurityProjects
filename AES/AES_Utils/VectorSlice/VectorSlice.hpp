#ifndef VECTOR_SLICE
#define VECTOR_SLICE
#include<vector>
using namespace std;
void sliceVector(const vector<unsigned char>& originalVector, vector<unsigned char>& result,const int&startWith);
#endif